import React, { useState } from 'react'

const Cart = (props) => {
    const [displayedCart,setDisplayedCart] = useState([])
    const {cartArray} = props;

    

    return (
        <div>
            
        </div>
    )
}

export default Cart
